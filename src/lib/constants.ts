/** Decimal precision for rounding converted values. */
export const FIXED_POINT = 3;

/** Base font size in pixels (browser default). */
export const FONT_SIZE = 16;

/** Pixels per inch (standard screen resolution). */
export const PPI = 96;

/** Default viewport width in pixels (Full HD). */
export const VIEWPORT_WIDTH = 1920;

/** Default viewport height in pixels (Full HD). */
export const VIEWPORT_HEIGHT = 1080;

/** Character width (ch) to em ratio - typical for most fonts. */
export const CH_TO_EM_RATIO = 0.5;

/** X-height (ex) to em ratio - typical for most fonts. */
export const EX_TO_EM_RATIO = 0.5;

export const GOLDEN_RATIO = 1.61803;

export const GOLDEN_RATIO_SQUARED = GOLDEN_RATIO ** 2;

export const ROOT_TWO = 1.41;

export const ROOT_TWO_SQUARED = ROOT_TWO ** 2;
